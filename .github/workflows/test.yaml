name: Test Repository Dispatch
on:
  repository_dispatch:
    types: [manual_test]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The webhook was triggered successfully!"
